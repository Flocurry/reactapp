import React, { Component } from 'react';

class Paginator extends Component {
    render() {
        return (
            <div>
                Paginator
            </div>
        );
    }
}

export default Paginator;